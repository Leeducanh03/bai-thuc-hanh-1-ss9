let year = +prompt("Nhập vào số năm ");
if (year % 4 == 0)
    if (year % 100 == 0) {
        if (year % 400 != 0) {
            isLeafYear = false;
        } else {
            // nhuận
        }
    } else {
        // không nhuận
    }
